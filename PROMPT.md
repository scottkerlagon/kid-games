# Starting Prompt

Copy-paste this into a fresh Claude Code session opened in the `kid-games/` root folder:

---

Build the launcher page (`index.html`) following the CLAUDE.md spec. This is a single HTML file (inline CSS is fine, no separate files needed) with three large game cards linking to:

- `math-game/index.html` — "Math Game" with green (#4CAF50) card
- `typing-game/index.html` — "Typing Game" with blue (#2196F3) card
- `coding-game/index.html` — "Coding Game" with orange (#FF9800) card

Each card should have the game name, a one-line description, and be a large clickable area. Kid-friendly design: big text, bright colors. Open it in the browser and verify the links work.
