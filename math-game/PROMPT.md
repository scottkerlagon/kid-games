# Starting Prompt

Copy-paste this into a fresh Claude Code session opened in the `math-game/` folder:

---

Build the math game from scratch following the CLAUDE.md spec exactly. Create all three files: `index.html`, `style.css`, and `app.js`.

Build order:
1. `index.html` — markup skeleton with all four screen divs (profile select, grade select, game screen, results)
2. `style.css` — full styling with green theme, 2×2 answer grid, 3×3 grade grid, kid-friendly sizes
3. `app.js` — everything: profile CRUD, screen navigation, question generators for all 9 grades (K-8), distractor generation, game session flow, difficulty tier promotion, localStorage persistence

After building all three files, open the game in the browser and verify it works by checking for console errors. Run through the verification checklist in CLAUDE.md.
